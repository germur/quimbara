---
import { weightClasses } from '@/data/weightClasses'
---

<div class='grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4'>
  {
    weightClasses.map((wc) => (
      <a
        href={`/fighters/${wc.slug}/`}
        class='bg-white p-6 rounded-2xl hover:shadow-lg transition-all duration-300 group flex flex-col items-center text-center border border-border hover:border-quimbara-magenta'
      >
        <h3 class='font-heading font-bold text-quimbara-purple text-lg mb-1 group-hover:text-quimbara-magenta transition-colors'>
          {wc.nameEs}
        </h3>
        <p class='text-xs text-quimbara-purple/60 mb-3 uppercase tracking-wider'>{wc.nameEn}</p>
        <div class='mt-auto pt-3 border-t border-border w-full'>
          <p class='text-xs text-quimbara-magenta font-mono font-bold'>{wc.limit}</p>
        </div>
      </a>
    ))
  }
</div>
